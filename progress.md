# earth-3d — progress

Файл: `index.html` (один файл, three.js r0.128 с CDN, ~840 строк).

## Сделано (последняя сессия)
- МКС: скорость орбиты ×0.1 (issOrbit.speed=0.05), добавлена в selectable[] —
  выбирается кликом (в центр камеры), как планета; клик по ней ещё и
  запускает космонавта (cosmonautActive), клик по issHit (увеличенная зона)
  тоже работает.
- Клик по названию (label) теперь работает для ВСЕХ selectable-объектов,
  включая МКС (не только планеты/Луна/Солнце).
- Подписи городов/объектов на Земле скрываются, если их ширина ≥ 1/5
  экранного диаметра Земли (updateMarkers, widthLimit = earthDiaPx/5) —
  чтобы издалека Земля не превращалась в "стену" из текста.
- Луна: реальная текстура (moon_1024.jpg) с bump вместо плоского серого цвета.
- Планеты солн. системы: процедурные canvas-текстуры (полосы для газовых
  гигантов, "кратеры" для каменистых), MeshPhongMaterial (реагируют на свет),
  осевое вращение (spin), кольца Сатурна — радиальная текстура.
- Земля: анизотропная фильтрация текстур, геометрия 128×128 (было 64),
  усиленная кривая bump/shininess/specular при приближении камеры.
- Автовращение Земли больше не останавливается при зуме колесом — только
  при ручном перетаскивании (mousedown/touchstart), возобновляется через
  ~4с простоя (idleTimer).
- Морские маршруты (танкеры/корабли) — ломаные через реальные проливы
  (Ормузский, Малаккский), огибают сушу, а не прямая между портами.
- Космодромы (5 шт) + ракеты: старт → подъём → перелёт к МКС → стыковка 8с
  → отстыковка, повтор раз в 15-35с.

## Архитектура (кратко)
- Земля/облака/ночные огни/атмосфера — в начале файла.
- CITIES/LANDMARKS/PLANTS/RU_CITIES/COSMODROMES — данные маркеров,
  addMarker() создаёт dot+label+hit, tierOf() — LOD видимости подписей.
- Спутники/самолёты/корабли/танкеры — мировые координаты или дочерние earth.
- Солнечная система (PLANETS, solarBodies) + selectable[] (включая МКС) —
  клик по мешу или label меняет center камеры (selectTarget()).
- animate() — общий тик, все update* вызываются там.

## TODO / идеи на будущее
- Позиция стыковки ракеты с МКС — условный оффсет -0.05 по X, не привязана
  к конкретному порту стыковки.
- Ships/tankers waypoints — приближённые, не точные судоходные линии/API.
- В удалённый git не пушить без явного запроса пользователя.

## Сделано (текущая сессия)
- Файл разбит: index.html (разметка + CDN three.js + <script type="module"
  src="js/main.js">) + js/data.js (все данные) + js/main.js (вся логика).
- js/data.js: CITIES, LANDMARKS, PLANTS, RU_CITIES, COSMODROMES, PLANETS
  (colors.rocky/banded вместо инлайн tex()), CONSTELLATIONS — было 6, стало
  12 (добавлены Телец/Альдебаран, Лев/Регул, Лира/Вега, Близнецы/Кастор,
  Персей/Мирфак, Стрелец/Каус Аустралис). Координаты звёзд приближённые.
  Альфы созвездий уже были подписаны/выделены жёлтым (alpha/alphaIdx) —
  логика перенесена без изменений, работает для всех 12.
- js/main.js: planetTexture(p) выбирает makeRockyTexture/makeBandedTexture
  по p.colors.{rocky|banded} — раньше было через инлайн-функцию в data.
- ⚠️ Не проверено в браузере (нет визуальной проверки после рефакторинга) —
  стоит открыть index.html и убедиться, что глобус/созвездия/планеты рендерятся.
- Добавлена панель справа (js/ui.js, initUI()): слайдер скорости для
  выбранного объекта (objectSpeed: Map<mesh,mul>, влияет на earth.rotation,
  updatePlanets/updateMoon/issPosAt), чекбоксы Созвездия/Города(city+ru)/
  Электростанции(plant, "АЗС" из запроса трактовано как электростанции —
  других данных нет)/Планеты/Названия(CSS body.hide-labels), кнопка Пауза
  (paused → effDt=0, simTime не растёт, camera/controls работают).
- Исправлен баг: updatePlanets() строил орбиты вокруг origin (0,0,0, где
  Земля), а не вокруг sunMesh.position (-160,15,-60) — планеты визуально
  толпились у Земли, хотя dist уже был пропорционален реальным расстояниям.
  Теперь позиция = sunMesh.position + (cos(a)*dist, 0, sin(a)*dist).
  ⚠️ Не проверено в браузере.
