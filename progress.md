# Progress — earth-3d refactor

## ✅ Рефакторинг завершён

### Все модули готовы:
- ✅ geo.js — latLonToVec3, orbitPoint
- ✅ state.js — SETTINGS, raycaster, VIS, speed, pause, distScale, autoRotate
- ✅ flags.js — флаги стран для маркеров
- ✅ tooltip.js — тултип для hover
- ✅ scene-core.js — scene, camera, renderer, controls, lights
- ✅ earth.js — earthMesh, rotation
- ✅ markers.js — города/столицы/заводы/созвездия/космодромы
- ✅ satellites.js — спутники вокруг Земли
- ✅ planes.js — самолёты
- ✅ seacraft.js — корабли
- ✅ sky.js — звёзды, млечный путь, туманности
- ✅ zoom.js — плавный зум
- ✅ solar-system.js — Солнце + планеты
- ✅ moon.js — Луна + орбита
- ✅ iss.js — МКС + космонавт
- ✅ rockets.js — ракеты с космодромов
- ✅ selection.js — selectTarget(), клики, init(ui)
- ✅ **main.js переписан с нуля** — импорты всех модулей, animate(), initUI wiring

### Готово:
- ✅ Удалён js/_extract/
- ✅ Удалены extract.py, find_markers.py
- ✅ Создан ARCHITECTURE.md
- ✅ Старый main.js сохранён как main.old.js
- ✅ Dev-сервер работает на http://localhost:8080/index.html

### Следующий шаг:
**Открыть http://localhost:8080/index.html в браузере и проверить работу**

### Исправление загрузки
- ✅ Исправлена ошибка `Identifier 'issLabel' has already been declared` в `js/markers.js`: удалено повторное локальное объявление, сохранен экспортируемый binding для `setIssLabel()`.

### Исправление бага #15
- ✅ Баг: при выборе планеты/объекта слайдер скорости вращал весь мир вокруг объекта
- ✅ Исправлено: слайдер теперь управляет глобальной скоростью симуляции
- ✅ Один слайдер ускоряет ВСЁ: вращение Земли, орбиты планет, транспорт, МКС
- ✅ Камера следует за выбранным объектом синхронно, мир не крутится

### Исправление загрузки
- ✅ Восстановлен экспорт `speedMul()` из `js/state.js`; Луна и МКС используют глобальную скорость симуляции без ошибки ES-модуля.

## Issue #16: Отображение флагов
- Добавлен тумблер "Флаги" (VIS.flags) в панель управления (после Столиц) и в Настройки (DEFAULTS.visFlags, settings.js).
- markers.js: флаг-спрайт столицы теперь отдельно управляется m.flagSpr.visible (facing && VIS.flags).
- Названия столиц скрываются при zoom ближе FLAG_LABEL_HIDE_DIST = 3.2*0.7 = 2.24 (30% ближе стартовой дистанции камеры), флаг остаётся виден.
- ⚠️ Не проверено в браузере.

## Прозрачность фона столиц
- Значение по умолчанию `capitalBgOp` в `js/settings.js` установлено в 20% непрозрачности (80% прозрачности).
- CSS-переменная `--capital-bg` применяется настройками через `localStorage`.

## Issue #16 fix: развязка Флаги/Столицы
- Флаг столицы больше не зависит от VIS.capitals: rawFacing (только направление, без kindVisible) + VIS.flags.
- dot.visible = facing || showFlag, чтобы сфера-контейнер флага рендерилась даже при выключенных Столицах.
- ⚠️ Не проверено в браузере.

## Issue #19: подписи столиц поверх Луны
Причина: CSS2DRenderer рисует подписи поверх WebGL без учёта глубины;
проверка facing была только относительно Земли, без учёта Луны.
Фикс: js/markers.js — occludedByMoon() (луч камера→маркер vs сфера Луны),
применена в updateMarkers() к opacity подписи и к showFlag (флаги столиц тоже скрываются).
⚠️ Не проверено в браузере (http://localhost:8080/index.html).

## Прозрачность флагов
- `js/flags.js`: прозрачность спрайтов флагов установлена на 25% (`opacity: 0.75`).

## Размер флагов
- `js/markers.js`: размер спрайтов флагов уменьшен на 30% пропорционально, с `0.045` до `0.0315` по ширине и высоте.

## Issue #23: Траектории орбит планет и Луны
- state.js/settings.js/ui.js: новый тумблер VIS.orbits (default true), чекбокс "Траектории" в панели и настройках (аналог остальных VIS-флагов).
- solar-system.js: для каждой планеты создана линия-окружность (THREE.Line, единичный радиус), в updatePlanets() позиционируется в sunMesh.position и масштабируется на d=dist*distScale — учитывает как орбиту вокруг смещённого Солнца, так и слайдер "Расстояние Земля–Солнце".
- moon.js: линия орбиты Луны статична (не завязана на distScale), построена один раз с той же формулой позиции, что и updateMoon().
- ⚠️ Не проверено в браузере.

## Issue #18: Парашют — капсула сбоку
- Fix: capsule.rotation.x=PI/2 (конус был развернут боком к направлению спуска)
- chute: добавлен наклон WIND_TILT=17° (сдвиг вперёд от ветра)
- Файл: js/rockets.js
- ⚠️ Не проверено в браузере


## Issue #16 доп.: множитель появления названий столиц вынесен в Настройки
- settings.js: поле "Множитель появления названий столиц" (s_capitalLabelHideMul), DEFAULTS.capitalLabelHideMul = 0.4417 (по умолчанию — в 3 раза ближе к поверхности Земли, чем было раньше: было 2.24 от центра при R=1, dist-от-поверхности 1.24 → стало 0.4133 → 3.2*0.4417≈1.4133).
- markers.js: FLAG_LABEL_HIDE_DIST теперь = 3.2 * SETTINGS.capitalLabelHideMul (было хардкод 3.2*0.7).
- ⚠️ Не проверено в браузере.


## Issue #16 доп2: исправлена перевёрнутая логика появления названий столиц
- Баг: FLAG_LABEL_HIDE_DIST-логика была перевёрнута — названия столиц были видны при старте (Земля целиком на экране) и скрывались при приближении. Ожидалось наоборот.
- Фикс: markers.js — CAPITAL_LABEL_APPEAR_DIST (было FLAG_LABEL_HIDE_DIST), условие hideCapitalLabel = dist >= CAPITAL_LABEL_APPEAR_DIST (было dist <). Теперь название появляется только когда камера БЛИЖЕ этого значения.
- settings.js: поле переименовано capitalLabelHideMul → capitalLabelAppearDist. Раньше было множителем от стартовой дистанции 3.2 (значение 0.4417, нечитаемо). Теперь это прямое значение дистанции камеры от центра Земли (диапазон 1.0–3.2, шаг 0.1), по умолчанию 1.4.
- 1.0 = поверхность Земли, 3.2 = стартовая дистанция камеры (Земля целиком на экране).
- ⚠️ Не проверено в браузере.
